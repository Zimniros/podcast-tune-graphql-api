type User {
  id: ID! @id
  name: String!
  email: String! @unique
}

type Category {
  id: ID! @id
  itunesId: Int!
  name: String!
}

type Podcast {
  id: ID! @id
  itunesId: Int!
  title: String!
  author: String!
  description: String!
  artwork: String!
  feedUrl: String!
}
